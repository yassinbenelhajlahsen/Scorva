{
  "name": "scorva",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -n backend,frontend,redis -c blue,magenta,red --kill-others-on-fail \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\" \"redis-server\"",
    "verify": "npm --prefix backend run verify && npm --prefix frontend run verify"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
