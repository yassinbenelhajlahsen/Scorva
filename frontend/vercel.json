{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "git diff HEAD^ HEAD --name-only -- . | grep -qE '.'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
