{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",

  "ignoreCommand": "git diff --quiet HEAD~ HEAD -- frontend/ public/ index.html vite.config.js package.json package-lock.json",

  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}